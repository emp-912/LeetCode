
class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        index = 0
        while i in len(nums):
            if nums[i] != val:
                index[i] = nums[i]
                index += 1
            return index
